---
title: "HTTP Methods"
summary: "Learn how HTTP methods describe client intent in API requests."
kind: "lesson"
status: "published"
level: "beginner"
durationMinutes: 20
objectives:
  - "Explain why methods communicate intent."
  - "Compare common HTTP methods used by backend APIs."
  - "Choose a method that matches a resource operation."
outputs:
  - "HTTP method intent table"
  - "Method selection notes for a simple API"
tags:
  - "backend-engineering"
  - "http"
  - "api-design"
---

## Method Intent

HTTP methods communicate the action a client wants to perform against a resource.

## Common Methods

`GET` retrieves data, `POST` creates or submits data, `PUT` replaces data, `PATCH` updates part of a resource, and `DELETE` removes a resource.

## Backend Design

Clear method usage makes APIs easier to test, document, cache, and integrate with.
